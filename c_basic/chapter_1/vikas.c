#include <stdio.h>

int main()
{
    // int a;
    int a =5;

    // float b = 1.5;
    float b;
    b = 1.5;

    char c ='a';
    c = 'a';

    // printing the value
    printf("The value of a is %d");
    printf("the vlue of b is %f");
    printf("the value of c is %c");
    
    // type of constants
    // integer constants - 1,2,4,5
    // real constant -23.5,55.6,34.2
    // character constant - '@','$','@'


// key word reserved

// auto, double, int, struct, break, long, else,
// switch,case, return, enum, typedef,char, register,
// extern, union, const, short, float, unsigned, continue,
// signed, for, void, default, sizeof, goto, volatile, do
// static, if, while

}
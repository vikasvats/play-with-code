#include <stdio.h>

int main(){
// writing a code for the area of circle with radius
    int r = 6;
    int height = 10;

    printf("the area of circle with radius %d is %f \n" , r, 3.14*r*r);
    printf("the volume of cylinder whose radius  %d and height %d,there volume is %f", r, height, 3.14*height*r*r);
    return 0;
}

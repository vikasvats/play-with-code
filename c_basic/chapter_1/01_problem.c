#include <stdio.h>
// writing a code to find the area of rectangle here is the code

int main(){
    int length ;
    int breadth ;
    printf("Enter length\n");
    scanf("%d",&length);
    printf("Enter breadth\n");
    scanf("%d",&breadth);
    printf("the area of rectangle is %d",length*breadth);
    return 0;
}
